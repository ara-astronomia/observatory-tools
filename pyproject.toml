[project]
name = "observatory-tools"
version = "0.2.0"
description = "Strumenti di pianificazione osservativa — Frasso Sabino"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.110.0",
    "uvicorn[standard]>=0.29.0",
    "sqlalchemy>=2.0.0",
    "pydantic>=2.0.0",
]
